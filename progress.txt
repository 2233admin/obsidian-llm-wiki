# vault-mind Progress

## What done (2026-04-08, session 7 -- v1.0.0 SHIPPED to GitHub)

- 18 commits' author/committer email rewritten via filter-branch:
  `1497479966@qq.com` -> `2233admin@users.noreply.github.com`
- v1.0.0 tag recreated with no-reply tagger email (was tagged by QQ email)
- gitea-fix remote removed (token leak resolved)
- GitHub origin added: https://github.com/2233admin/obsidian-llm-wiki.git
- Old remote master (v0.1.0 Obsidian-plugin era) preserved as
  `legacy-v0.1.0-obsidian-plugin` branch
- Local main pushed to origin, default branch switched to main
- Old master branch deleted from GitHub
- v1.0.0 tag pushed
- Repo state on GitHub: 5 stars, 3 forks (4 days organic)
- Local cleanup: refs/original/, backup branch, github-tmp/master removed

## Branch / tag layout

- `main` (default) -- v1.0.0 headless-first MCP architecture (18 commits)
- `legacy-v0.1.0-obsidian-plugin` (archive) -- v0.1.0 Obsidian-plugin era,
  1 commit, retained for historical reference
- tag `v1.0.0` -- points to docs commit (CHANGELOG + RELEASE_NOTES)
- tag `v0.1.0` -- still points to legacy commit, retains QQ email in
  the original commit object (acceptable per A-plan: water already
  spilled when v0.1.0 was first published 4 days ago)

## 2026-04-10 -- A-line wrapup session

### What done (discoveries + small edits)

- **GitHub release v1.0.0 already existed** (published 2026-04-08T10:07:34Z,
  body == RELEASE_NOTES.md). progress.txt previous "Outstanding" item was
  stale. https://github.com/2233admin/obsidian-llm-wiki/releases/tag/v1.0.0
- **Global ~/CLAUDE.md Project + Stack blocks rewritten** to reflect v1.0
  reality: now distinguishes vault-mind (headless MCP, main) from
  obsidian-vault-bridge (plugin, master). Stack block pruned from 64 lines
  to ~22 lines (est. -2000 tokens/session). Committed separately in home
  config repo.

## 2026-04-13 -- Track 2a ObsidianAdapter complete

### What done

- ObsidianAdapter implemented: `mcp-server/src/adapters/obsidian.ts`
  - JSON-RPC 2.0 over WebSocket, reads `~/.obsidian-ws-port` for port+token
  - search/read/write/graph/events -- all 5 capabilities
  - write() tries vault.modify, falls back to vault.create on -32001
  - Graceful degradation: init() always resolves, isAvailable=false on failure
  - rejectAllPending() cleans up in-flight RPCs on WS close or dispose
- ObsidianAdapter registered in index.ts (enabledAdapters.has("obsidian") gate)
- Tests: `mcp-server/src/adapters/obsidian.test.ts` -- 20/20 pass
  - 3 suites: unavailable paths (8), mock WS server (11), auth rejection (1)
  - Ports 60001/60002 (Windows reserved range 49876+ was EACCES)
- Commits: 545996d (impl) + 8efeff0 (tests), pushed to GitHub

## 2026-04-13 -- Phase 1A: Contract-first operations.ts COMPLETE

### What done

- Created `mcp-server/src/core/types.ts` -- shared interfaces: Operation, ParamDef,
  OperationContext, VaultBackend (with execute()), Logger, SearchResult, FileStat, etc.
- Created `mcp-server/src/core/validate.ts` -- param validation with type coercion
- Created `mcp-server/src/core/operations.ts` -- 20 vault.* operations, single source of
  truth; all handlers delegate via ctx.vault.execute()
- Created `mcp-server/src/connector/ws-transport.ts` -- TS port of JS WsTransport
- Created `mcp-server/src/connector/fs-transport.ts` -- TS port of JS FsTransport (all 20 cases)
- Created `mcp-server/src/connector/connector.ts` -- operations-driven MCP connector,
  raw JSON-RPC 2.0 (no SDK), vault.* only, WS-first/FS-fallback
- Updated `mcp-server/src/index.ts` -- ListTools/CallTool auto-generated from operations[],
  VaultFs.execute() added, compile trigger preserved, 4 missing dispatch cases added
- Deleted `mcp-server/src/connector.js` (original 32KB JS file)
- All 31 tests pass throughout; typecheck 0 errors

## 2026-04-14 -- Phase 1B: Recipe framework + x-to-vault COMPLETE

### What done

- `recipes/collectors/x-collector.ts`: fix epoch bug (x-rate-limit-reset is Unix epoch,
  not delta; old code would wait ~54 years). commit 2ed385b
- `mcp-server/src/recipes/_framework.ts`: removed unused `dirname` import. commit 80eb9dd
- `mcp-server/src/recipes/_registry.ts`: added lazy cache (module-level `_cache`),
  `recipesDir` param bypasses cache for tests. commit 03873b3
- `mcp-server/src/recipes/recipes.test.ts`: 33 unit tests across 7 describe blocks
  (parseRecipe valid/invalid, getRecipeStatus, runHealthCheck, appendHeartbeat,
  scanRecipes, findRecipe). Uses node:test + tmpdir isolation. commit a050c0c
- Karpathy simplicity cleanup: removed speculative `resetRecipeCache` export,
  removed unimplemented "Topics Detected" placeholder from buildDigest. commit 389f013
- All 64 tests pass. All 5 commits pushed to GitHub main.

## 2026-04-15 -- voile-to-vault recipe COMPLETE

### What done

- Investigated Voile (github.com/2233admin/voile): QQ+WeChat ingestion system
  - Python+Go+Rust, writes to E:/knowledge via ObsidianWriter
  - Schema: messages/message_topics/message_sentiments/links tables (SQLAlchemy)
  - Voile DB not yet set up; collector ready to activate when it is
- `recipes/collectors/voile-collector.ts`: Bun-native Postgres collector
  - `import { sql } from 'bun'` -- zero npm deps, Bun 1.2+ native
  - Auto-discover channels (SELECT DISTINCT) + VOILE_CHANNELS override
  - since_id cursor per channel, checkpoints after each channel
  - Graceful skip if message_topics/message_sentiments not yet populated
  - Graceful error on DB connect failure (exit 1 with helpful message)
- `recipes/voile-to-vault.md`: recipe frontmatter + setup docs
- All 64 existing tests still pass. commit 83a2355.

## 2026-04-15 -- feishu-to-vault collector COMPLETE

### What done

- `recipes/collectors/feishu-collector.ts`: REST API collector (tenant_access_token)
  - Auth: POST /auth/v3/tenant_access_token/internal, token cached in-memory (2h TTL)
  - Auto-discover all group chats bot is in (GET /im/v1/chats) + FEISHU_CHATS override
  - since_time cursor per chat (Unix seconds), checkpoints after each chat
  - Content extraction: text/post (rich text flattened) + media placeholders
  - Graceful skip on code=230002/230003 (bot not in chat or missing scope)
  - Required app scopes: im:message:readonly + im:chat:readonly
- `recipes/feishu-to-vault.md`: recipe frontmatter + setup docs
- Transpile verified: auth call reaches Feishu API, correct 10003 on bad creds
- All 64 existing tests still pass. commit 17b9bf3.

## 2026-04-15 -- Batch collector session: 5 new sources COMPLETE

### What done

- `recipes/collectors/napcat-collector.ts`: OneBot v11 HTTP (NapCatQQ)
- `recipes/collectors/linear-collector.ts`: Linear GraphQL
- `recipes/collectors/wechatmsg-collector.ts`: WeChatMsg local HTTP
- `recipes/collectors/astrbot-collector.ts`: AstrBot SQLite (bun:sqlite)
- `recipes/collectors/weflow-collector.ts`: WeFlow WeChat HTTP
- 5 recipe .md files: napcat/linear/wechatmsg/astrbot/weflow-to-vault.md
- All 5 transpile-verified. All 64 tests still pass.

## 2026-04-15 -- Domestic adaptation COMPLETE (commit 6f6c0e9)

### What done

- `compiler/extractor.py`: PROVIDER_PRESETS dict (anthropic/qwen/doubao/minimax)
- `compiler/compile.py`: COMPILE_PROVIDER env var, auto-resolves base_url + model
- `mcp-server/src/core/operations.ts`: recipe.run operation added
- `.npmrc` + `bunfig.toml`: npmmirror.com CDN
- `.env.domestic.example`: one-stop template

## 2026-04-15 -- Phase 1B: .mcp.json wired + integration test COMPLETE

### What done

- `vault-mind.yaml`: created (gitignored) with vault_path: E:/knowledge, filesystem adapter
- `.mcp.json`: registers two servers:
  - `vault-mind` (index.js, all ops incl recipe.run, cwd=project root)
  - `vault-mind-connector` (connector/connector.js, vault.* only, WS/FS fallback)
- rebuild: dist was stale (recipe.run added in prev session but not compiled)
- fix: `_framework.ts` parseYaml inline branch: `key: []` now parses as empty array
  (was returning string "[]", causing astrbot-to-vault + wechatmsg-to-vault to be
  skipped as malformed on every scan)
- integration test: `recipe.run {id:"napcat-to-vault"}` returns correct
  `{"ok":false,"error":"Missing secrets: NAPCAT_TOKEN"}` -- MCP plumbing confirmed
- 64/64 tests pass. commit a91b239.
- VaultExecutor task: already done in prior session (VaultExecutor interface in types.ts,
  as VaultBackend cast no longer exists)

## 2026-04-15 -- Track 6 dedup + circleback-to-vault COMPLETE

### What done

- **Track 6 dedup (E:/knowledge)**: 44/44 files deleted, 0 errors
  - 33 [archive-copy]: live version exists, archive copy redundant
  - 5  [archive-dupe]: multiple copies all in archive, keep shortest path
  - 6  [live-dupe]: same basename in multiple live dirs, keep lowest dir-rank
  - 5 groups skipped (Excalidraw pairs, test fixtures, diff-name live files)
  - Script: C:/Users/Administrator/dedup.py (temp, not in project)
- **circleback-to-vault** (commit f47f8fb):
  - `recipes/circleback-to-vault.md`: recipe frontmatter, CIRCLEBACK_API_KEY,
    health check, 3-min setup guide
  - `recipes/collectors/circleback-collector.ts`: cursor-paginated /v1/notes fetch,
    incremental sync via state.json last_synced, groups meetings by date, formats
    summary + attendees + action items, appends if same-day digest already exists
  - 64/64 tests pass

### Outstanding

- Circleback activation: set CIRCLEBACK_API_KEY, run collector
- NapCat activation: set NAPCAT_TOKEN + NAPCAT_URL, run recipe.run
- Feishu/Voile/Linear/WeChatMsg/AstrBot/WeFlow: awaiting activation
- main/master branch strategy decision
- Optionally push main to gitea mirror

## 2026-04-15 -- gmail-to-vault collector COMPLETE

### What done

- `recipes/collectors/gmail-setup.ts`: one-time OAuth 2.0 browser flow
  - Builds auth URL (access_type=offline, prompt=consent), opens browser cross-platform
  - Local HTTP server on :9876 captures ?code= callback, 5-min timeout
  - Exchanges code for tokens; prints `export GMAIL_REFRESH_TOKEN="..."` and exits
- `recipes/collectors/gmail-collector.ts`: runtime incremental collector
  - `getAccessToken()`: POST grant_type=refresh_token -> access_token
  - `listMessages()`: /gmail/v1/users/me/messages?q=QUERY&after=EPOCH
  - `getMessage()`: format=metadata, extracts From/Subject/Date headers
  - Groups digest by sender domain (senderDomain extracts @domain.tld)
  - last_synced cursor in state.json for incremental sync (no duplicates)
  - Appends to same-day digest if it already exists
- `recipes/gmail-to-vault.md`: recipe frontmatter + 7-step setup guide
- Track 6 dedup (E:/knowledge): 44/44 files deleted, 0 errors (done this session)
- circleback-to-vault: committed this session (commit f47f8fb)
- All commits pushed to GitHub main (ce99ce0..9ad169e)
- All 64 existing tests still pass

### Collector inventory (10 sources, all implemented)

x / voile / feishu / napcat / linear / wechatmsg / astrbot / weflow / circleback / gmail
All awaiting activation (need credentials). X needs X_BEARER_TOKEN.

### Outstanding

- Gmail activation: run gmail-setup.ts to get refresh_token, then set 3 env vars
- All other collectors: set respective credentials (see each recipe .md)
- main/master branch strategy decision
- Optionally push Gitea mirror

## 2026-04-18 -- frontmatter_generator CLI (authored, not committed in that session)

### What done (retroactive record, session ended without DoD)

- `compiler/frontmatter_generator.py` (33KB, 5 subcommands)
  - `vocab`: scan vault, compute general tag pool -> general_tags.txt
  - `generate`: LLM-batch generate FM previews for files missing frontmatter
  - `apply`: atomic prepend + git commit the previews
  - `retry`: regenerate minimum-only FM with wider context + folder/filename hints
  - `audit`: scan existing FM, report non-canonical status/type + missing concepts
  - Zero-dep (stdlib + curl subprocess), ThreadPoolExecutor concurrency, ${ENV_VAR} substitution
- `compiler/fm_config.example.json`: template (anthropic/qwen/doubao/minimax presets,
  canonical status/type lists, folder_type_map, status_rules, tag_blacklist)
- `.gitignore`: add `compiler/fm_config.local.json` + `compiler/fm_work/` + `fm_work/`

## 2026-04-19 -- Roadmap planning session (this session)

### Context

- Competitive landscape scan triggered by Garry Tan's gbrain launch (2026-04-10, 24h
  5400 stars) + Karpathy LLM Wiki gist (2026-04) with 6+ community forks appearing
- Realization: gbrain is NOT a direct competitor (Postgres+pgvector heavy,
  Obsidian-agnostic). Real risk is isolation -- v1.0 at 5 stars/3 forks for 11 days
  with zero outside issues/PRs and no Curry daily dogfood loop
- Karpathy gist explicitly says "no embeddings at small scale" -- obsidian-llm-wiki's
  filesystem+grep path is the original orthodoxy. Position = "Karpathy LLM Wiki
  reference implementation, Obsidian-native, no Postgres"

### What done

- Full 12-week roadmap written to
  `C:/Users/Administrator/.claude/plans/breezy-stargazing-chipmunk.md`
- Four strategic tracks defined:
  - Track A: Dogfood (daily + weekly hooks, .dogfood.log)
  - Track B: Positioning (Karpathy reference impl, SEO, outreach drafts)
  - Track C: Compiler (link_discovery, concept_graph, topic_cluster -- make the
    architecture diagram real)
  - Track D: Collector activation (Gmail first, then 2 more)
- Phase 0 executed in this session:
  - Deleted stray `nul` file (7815B, bash redirect accident)
  - Committed frontmatter_generator + config example + .gitignore
  - This progress.txt update

### Outstanding (Week 1 remainder)

- README rewrite (Karpathy hero, gbrain/qmd comparison)
- package.json + GitHub topics SEO
- .outreach/{karpathy-gist-comment,x-thread,show-hn-draft}.md
- .dogfood.log init + scripts/dogfood-log.sh
- main/master branch decision (unchanged, still deferred)
- Optionally push Gitea mirror

## 2026-04-19 -- Week 2 Track A dogfood hard-start COMPLETE

### What done

- `.claude/settings.json` (project-scope, committed): registers two hooks
  - PostToolUse matcher `mcp__vault-mind.*` -> logs tool call + query
  - Stop -> logs session-end with 8-char session_id prefix
- `scripts/hooks/dogfood-hook.py` (stdlib-only, zero-dep):
  - Modes: `posttool` filters non-vault-mind tools, extracts query/path/id
  - Mode `stop` records session end
  - Non-blocking: exits 0 on any error (never breaks Claude Code)
  - Output: tab-separated `ts\ttool\top\tnote` appended to `.dogfood.log`
- `scripts/weekly-review.ps1`: reads last 7d of `.dogfood.log`, groups by op,
  appends dated block to `.dogfood.weekly.md` (gitignored)
- Windows scheduled task `obsidian-llm-wiki-weekly` registered via
  `Register-ScheduledTask` (State=Ready, weekly Sunday 03:00 local)
- Smoke test: 2 vault-mind calls + 2 session-end events logged; non-vault-mind
  Read call correctly filtered out; weekly-review.ps1 produced rollup
- `.gitignore`: add `.dogfood.weekly.md`

### Outstanding (Week 2 remainder)

- `/vault-save` session-end binding (separate from dogfood log -- actually writes
  a recap note to the vault). Not done this session.
- Gitea mirror push (optional)
- main/master branch strategy decision (still deferred)

## 2026-04-20 -- Week 2 Track A /vault-save session-end binding COMPLETE

### What done

- `scripts/hooks/vault-save-hook.py` (stdlib-only, ~170 LOC, non-blocking):
  - Reads Stop payload, parses transcript JSONL via `transcript_path`
  - Extracts: user prompts, vault-mind writes, Edit/Write file paths, git commit
    subjects (regex from Bash `git commit -m "..."`)
  - Skip criteria: <3 user msgs AND no vault writes AND no file edits AND no
    commits -> ephemeral, no recap (but mark seen)
  - Writes recap block to `E:/knowledge/06-Daily/YYYY-MM-DD.md` (appends if
    exists, creates with frontmatter if missing)
  - Idempotent via `.vault-save.log` (seen session_ids, per-dev, gitignored)
  - Failure fallback: `.vault-save.failed.log` (errors never break Claude Code)
- `.claude/settings.json`: vault-save-hook registered AFTER dogfood-hook in
  Stop array (same hooks block, sequential execution)
- `.gitignore`: add `.vault-save.log` + `.vault-save.failed.log`
- Smoke tests (all pass):
  1. Substance session (3 prompts + Edit + commit) -> recap written to daily
  2. Same sid rerun -> no duplicate (idempotent)
  3. Ephemeral session (1 prompt, no tools) -> skipped, sid still marked seen

### Design notes

- Stop hook runs shell, not Claude -- cannot invoke `/vault-save` slash command
  directly. Hook does the extraction+write mechanically from the transcript.
  `skills/vault-save.md` remains as the interactive richer version for manual
  `/vault-save` invocation; hook is the unattended fallback.
- Windows Python Path('/tmp/...') quirk discovered during smoke test: Git Bash
  `/tmp` != Python tmp. Hook uses absolute paths only.

### Outstanding (Week 2 remainder)

- Optionally push to Gitea mirror
- main/master branch strategy decision (still deferred)

## 2026-04-20 -- Track C Compiler: link_discovery.py skeleton COMPLETE

### What done

- `compiler/link_discovery.py` (~250 LOC, stdlib-only):
  - Pass 1 literal alias match (Karpathy grep-first, no LLM, no embeddings)
  - Index: file stem + frontmatter `aliases` + H1 heading
  - Min-alias length: 4 ASCII / 3 CJK (guard against "历史" firing on 历史学/etc)
  - CJK gets raw substring, ASCII gets `\b` word-boundary regex
  - Skip zones before match: frontmatter + fenced code + inline code +
    existing `[[wikilinks]]` + `[md](links)`
  - First-wins index keeps runs idempotent across vault moves
  - `--with-llm` flag reserved for Phase 2 (stubbed, prints notice)
  - Default output: **project-local** `.compile/link_suggestions.md` (NOT
    inside vault -- avoids Obsidian indexing pollution)
- `compiler/tests/test_link_discovery.py`: 13 unit tests covering frontmatter
  parsing, index building, scan correctness, CJK vs ASCII matching, noise
  skipping, end-to-end report generation
- `.gitignore`: add `.compile/` (per-dev build artifact)

### Smoke test on real vault (E:/knowledge)

- 554 md scanned (archive/templates/excalidraw/KB/obsidian excluded)
- 967 aliases indexed
- 2403 link suggestions in 10 seconds
- Quality spot-check: good hits (`量化交易`->Stars-量化交易, `记忆系统`,
  `OpenClaw`, `FORGE`). Noise hits (`claude`, `skills`, `findings`) from
  over-generic filenames -- Phase 2 will add stopword/generality filter

### Design notes

- Build_index walks the full vault regardless of `--limit`; only scan pass
  respects limit. Keeps index complete across partial re-runs
- ASCII `\b` doesn't work for CJK (no whitespace separation), so CJK aliases
  fall through to raw `alias_lower in line_lower` -- acceptable false-positive
  cost traded for determinism; a better post-filter lives in Phase 2

### Outstanding (Week 3-4 remainder)

- `compiler/concept_graph.py` v0 -> `graph.json` (nodes=concept, edges=wikilink)
- LLM pass in link_discovery (requires extractor.py reuse + cost caps)
- Stopword/generality filter for noise aliases
- Unit test coverage target (roadmap says 60%+; skeleton hit ~80% for link_discovery)

## 2026-04-20 (evening) -- concept_graph.py v0 + MCP root-listing fix

### What done

- `compiler/concept_graph.py` (~200 LOC, stdlib-only):
  - Walks vault md, extracts `[[wikilinks]]` (strips fences, inline code,
    frontmatter), emits `graph.json` with `{nodes, edges, stats}`
  - Node: `{id, title, path, tags}` (id = vault-relative posix path)
  - Edge: `{src, dst, resolved}` -- unresolved links kept as edges with
    `resolved=false` so dangling references stay visible
  - Resolution via stem + frontmatter aliases + H1 (first-wins, case-insensitive)
  - Tags pulled from frontmatter (bracket + bullet list YAML subset, reuses
    link_discovery parser shape)
- `compiler/tests/test_concept_graph.py`: 13 unit tests (5 extract, 3 tags,
  4 build_graph, 1 e2e) -- all pass, 0.18s
- Smoke on real vault (E:/knowledge): 554 nodes / 523 edges / 120 unresolved
  in ~3s. Unresolved mostly pinyin-kebab stubs (xiang-mu-quan-jing etc.) --
  worth a cleanup backlog, not a bug.

### MCP vault.list root bug (dispatched to Codex, cold-verified by Claude)

- Repro: `vault.list({path:""})` -> "path required";
  `vault.list({path:"."})` -> "path traversal blocked"
- Root cause: `resolvePath` rejected empty + `.` before allowing either as
  "list vault root". Same bug lived in both `src/index.ts` (stdio MCP) and
  `src/connector/fs-transport.ts` (connector transport) -- Codex found and
  fixed both.
- Fix: split `resolvePath` into `normalizeVaultPath(opts: {allowRoot?})` +
  `resolve(opts: {allowRoot?})`; `vault.list` opts into `allowRoot`, other
  tools stay strict. Also replaced `full.startsWith(vault)` guard with
  `path.relative(vault, full).startsWith('..')` -- correct idiom on
  case-insensitive Windows FS, free hardening.
- Tests: 7 new node --test cases in `src/connector/fs-transport.test.ts`
  (root list x3, traversal reject x3, nested unchanged x1). Total mcp-server
  tests: 77/77 pass (cold-verified via `npm test` independently of Codex).
- Smoke (cold-verified by Claude): JSON-RPC roundtrip of `vault.list` with
  `{"":...,".":...,"/":...,"./":...}` all return real vault root;
  `{..:...,"C:/Windows":...,"../":...,"../etc":...}` all still blocked.

### Commits this session

- `fix(mcp): accept empty/root paths in vault.list, harden traversal guard`
- `feat(compiler): concept_graph.py v0 wikilink graph (stdlib-only)`
- `docs(progress): 2026-04-20 evening -- concept_graph + MCP root-listing fix`

### Outstanding

- `concept_graph.py` v0 is wikilink-only. Tags collected on nodes but no
  tag-edges yet. Phase 1.5: emit `{src: node, dst: 'tag:X', kind: 'tag'}`
  edges + a `tags[]` node list -> consumer can build cluster/facet views.
- `link_discovery --with-llm` still stubbed (Phase 2)
- `.omc/state/*` autogen files tracked in git (pre-existing). Worth
  `git rm --cached` + confirm .gitignore catches them next session.

## 2026-04-20 (late) -- gap closure: MCP exists/stat root + concept_graph tag edges + state untrack

### What done

- `chore(gitignore)`: untracked `.omc/state/hud-state.json`,
  `.omc/state/hud-stdin-cache.json`, `mcp-server/.omc/state/last-tool-error.json`
  (tracked before .gitignore rule existed, gitignore is not retroactive).
  Local hook-generated state no longer pollutes `git status` / diff noise.
- `fix(mcp)`: extended root-path support from vault.list to vault.exists +
  vault.stat. Same `normalizeVaultPath({allowRoot: true}) + resolve({allowRoot})`
  pattern. Enables client bootstrap probe ("does the vault exist and is it
  readable?") without a dummy subpath. Traversal guards unchanged.
  `stat("")` returns `{type:'folder', name: basename(vaultPath), children: N}`.
  + 7 node --test cases in fs-transport.test.ts (exists root x2, exists reject,
  stat root x2, stat reject, stat nested file). Total: 84/84 tests pass.
- `feat(compiler)`: concept_graph now emits tag edges. Each node's frontmatter
  tags materialize as `{src: node_id, dst: 'tag:X', resolved: true, kind: 'tag'}`.
  Wikilink edges tagged `kind: 'wikilink'` (previously implicit). Stats split
  `wikilink_edges` + `tag_edges` counts. Consumer synthesizes tag-nodes from
  distinct dst values where kind=='tag'.
  + 1 new test case; all 14 tests pass in 0.16s.
  Smoke on E:/knowledge: scanned=554 nodes=554 edges=2507 (wikilink=523
  tag=1984) unresolved=120. ~3.6 tags per node, matches vault reality.

### Cold-verification (per codex-dual-verify rule)

All MCP fixes verified independently via JSON-RPC roundtrip, not trusting
Codex or Claude own-report:
- `vault.exists({path:'' | '.'})` -> `{exists: true}`
- `vault.stat({path:''})` -> `{type:'folder', name:'knowledge', children:31}`
- `vault.exists({path:'../etc'})` -> blocked
- `vault.stat({path:'../etc'})` -> blocked

### Commits this block

- `chore(gitignore):` untrack .omc/state autogen files
- `fix(mcp):` extend root-path support to vault.exists and vault.stat
- `feat(compiler):` emit tag edges in concept_graph (v0.2)
- `docs(progress):` 2026-04-20 late -- gap closure

### Outstanding

- `link_discovery --with-llm` still stubbed (Phase 2, deferred)
- link_discovery stopword/generality filter deferred pending dogfooding
  noise data (by design -- let noise tell us which filters matter)
- 120 unresolved concept_graph edges are cleanup surface for the vault,
  not a bug

## Next step

- Phase 2 link_discovery: LLM-backed concept suggestion pass (requires
  extractor.py reuse + cost caps)
- Or: build a first consumer of graph.json (viz? backlink MCP tool? vault
  health report?) -- graph is now rich enough (2507 edges) to drive one
- Roadmap file: C:/Users/Administrator/.claude/plans/breezy-stargazing-chipmunk.md


## 2026-04-20 -- spec-D: Scaffold (setup + cytoscape viewer + vercel) COMPLETE

### What done

- `.compile/specD-scaffold/` produced per spec-D output contract:
  - `setup` -- POSIX bash script, works under Git Bash/macOS/Linux
    - `--host claude|codex|opencode|gemini` flag
    - `--dry-run` prints what it WOULD do, no side effects
    - Detects missing host dir and exits non-zero with actionable error
    - Installs skill files to `~/.{claude,codex}/skills/vault-wiki/` etc.
    - Prints .mcp.json snippet + CLAUDE.md personas table to stdout
    - Cleans up setup scripts from install target
    - Never uses sudo, never installs npm/pip
  - `setup.ps1` -- PowerShell sibling with same semantics
  - `viewer/index.html` -- static single-file page, no build step
  - `viewer/viewer.js` -- cytoscape + cose-bilkent loaded from unpkg CDN
  - `viewer/viewer.css` -- raw CSS, 104 lines (well under 200-line limit)
  - `viewer/sample-graph.json` -- pre-committed specB demo graph (10 nodes, 70 edges)
  - `vercel.json` -- static deploy pointing to viewer/, zero functions

### Acceptance checklist (all pass)

- [x] `bash setup --host claude --dry-run` prints dry-run + next steps, no side effects
- [x] `bash setup --host codex|opencode|gemini --dry-run` each prints plausible target dir
- [x] Setup fails cleanly if parent dir missing (actionable error, no stack trace)
- [x] Viewer: `grep -i 'require\|import' viewer/*.js` -- only CDN URL strings match
- [x] `vercel.json` has zero functions keys, `outputDirectory: viewer`
- [x] CSS is raw CSS, no tailwind/bootstrap (104 lines)
- [x] All 7 output files present

### Known risk

- Cytoscape + cose-bilkent loaded from unpkg CDN on each page open.
  Acceptable for demo; noted in README as "first load may feel laggy".

---

## Session handoff -- 2026-04-20 06:50 (v2 dispatch + integration)

### What done

- KT routed to MiniMax-M2.7-highspeed via OpenAI-compat path
  (api.minimaxi.com/v1, profile minimax-local, key in MINIMAX_API_KEY env).
- 5 worker dispatch executed via `kt run swe --mode cli --no-session`
  in 3 waves (A+B parallel; C+D parallel; E solo). All 5 specs PASSED
  HMS gates against acceptance checklists + reject signals. Total burn
  ~260k tokens, ~30 min wall-clock.
- v2-staging branch created off main (post bugfix commit 71b0492).
- 7 atomic commits on v2-staging integrating spec A-E outputs:
  - 524b339 docs(plan)         -- v2 requirements + 5 specs
  - e752ba9 feat(personas)     -- 6 skills + KT creature configs
  - cec28b6 feat(examples)     -- demo-vault (10 notes + graph)
  - 3f56883 docs(readme)       -- v2 README + WHY_NOT_JUST_GREP + INSTALL
  - d9a5e88 feat(scaffold)     -- setup + viewer + vercel
  - 39eba87 feat(kt)           -- terrarium + smoke + KT_DOGFOOD
  - 7c1729a fix(examples)      -- rebuild graph.json (unresolved=1 fix)
- Smoke verified: setup --dry-run all 4 hosts (claude/codex/opencode/gemini)
  prints correct target dirs + paste-able mcp.json + CLAUDE.md snippet;
  bad host fails clean. README 96 lines, tagline 10 words, 4 example
  prompts, MIT license. Demo graph rebuilds clean (11 nodes / 70 edges
  / unresolved=1).

### Known gaps (carried to next session)

- [P2] setup script copies whole repo (.git/.compile/mcp-server/node_modules
  inflates install). Should select skills/, examples/, docs/, mcp-server/
  (sans node_modules), terrariums/, viewer/ explicitly.
- [P2] docs/gif/demo.gif placeholder -- recording per spec-C gif-script.md
  is a separate task (pre-tag would be ideal).
- [P3] terrarium yaml uses flat layout (root/creatures/channels at top
  level, not nested under terrarium:). Worker's interpretation; verify
  against actual KT terrarium parser before declaring it "validated".
- [P3] smoke/03-smoke-script.sh is manual checklist mode -- automated
  end-to-end not yet executed; needs MINIMAX_TOKEN export + kt installed
  on whatever box runs it.
- [P3] M2.7-highspeed + KT sub-agents = broken (tool_id mismatch).
  Workers use direct tools only via prompt guardrail. Worth filing
  upstream issue against KT or MiniMax provider.

### Next step

1. Eyeball v2-staging diff once more (`git log --oneline main..v2-staging`).
2. `git push -u origin v2-staging`.
3. `git tag v2.0.0-rc1 && git push origin v2.0.0-rc1` (announces v2 line
   without disturbing v1.0.0 stable tag).
4. `vercel deploy --prod` against viewer/ (may need `vercel link` first).
5. Record demo GIF per docs/gif-script.md.
6. Address P2 follow-ups (setup leanness + GIF) before promoting v2-staging
   -> main as v2.0.0 final.


## 2026-04-20 evening session 2 -- install leanness + viewer fixes + strategic pivot

### What done (2 atomic commits on v2-staging)

- 9d790c7 feat(install): bundle MCP server + lean allowlist + restore MIT
  - esbuild --bundle dist/index.js -> mcp-server/bundle.js (1.5 MB,
    self-contained, ESM with createRequire shim for ws CJS interop)
  - npm run bundle + npm run rebuild scripts; main switched to bundle.js
  - setup / setup.ps1: explicit allowlist (skills, examples, docs,
    terrariums, viewer, smoke, top-level docs, mcp-server/{bundle.js,
    package.json}). Fail fast if bundle missing. PS variant uses
    -VaultHost (not -Host -- $Host is reserved in PowerShell).
  - README quick-start fixed (clones to sibling dir; old command
    cloned full repo into install dir, bypassing setup entirely)
  - docs/INSTALL.md rewritten v1 -> v2 (v1 referenced npm install +
    tsc + yaml prompt + ~/.claude.json registration + smoketest --
    none of which the current setup actually does)
  - LICENSE restored from first commit d0f212c (was lost when v2
    work started on a fresh branch). License inconsistency fixed:
    package.json said GPL-3.0 (stray scaffold) while README + legacy
    LICENSE both said MIT. README/intent wins; corrected to MIT.
  - .gitignore: ignore mcp-server/smoke.{in,out,err} test artifacts
  - Verified: install 1.6 MB / 406 ms (was 64 MB), boot 76 ms,
    MCP initialize + tools/list + vault.list all valid JSON-RPC

- 1b153d9 fix(viewer): graph actually renders -- 4 bugs into a blank canvas
  - Before: stats said "nodes: 10 / edges: 70" but canvas was empty
  - Drop cose-bilkent CDN chain (needs layout-base + cose-base loaded
    first); switched to cytoscape's built-in cose layout (zero extension).
  - Materialize ghost nodes for missing edge targets; classify by
    edge.kind so tags get tag style, real missing notes get
    unresolved style.
  - Stop swallowing render errors. Old .catch(()=>{}) masked every
    cytoscape failure. Now distinguishes 404 (silent) from real errors.
  - Add styling for kind=unresolved (dark fill, dashed red border).
  - Verified in headless playwright at 1440x900 + 390x844: real graph
    renders, 10 concept nodes (blue) + 17 tag nodes (purple) + 70 edges
    + 1 unresolved link as dashed-red ghost. Zero console errors.

### Strategic pivot (key for next session)

Surface read of UX work shifted twice:

1. After viewer fix, proposed Tier 1-3 of further viewer polish.
2. Cross-checked with Codex (TTY-blocked, but agent self-assessed) +
   GitHub research. Found Juggl (HEmile/juggl) -- mature
   cytoscape-based Obsidian graph plugin doing what we were
   independently rediscovering. Codex flagged "bake demo graph
   inline + click-highlight neighbors" as highest leverage.
3. Curry pushed back fundamentally: "前端不是必须要的 跟你自然交互
   才是最好的解决方案" = the conversation IS the interface, not the
   viewer/frontend. Karpathy LLM Wiki philosophy: vault is the
   precipitate of conversations, not a thing you visualize.

### What blocked

None. Strategic direction confirmed: stop investing in viewer
beyond 1b153d9, refocus on conversation quality (personas + MCP
tool descriptions + first-prompt UX + cold-start for empty vaults).

### Next step (next session opener -- not started)

1. Audit pass on conversation surface (~150 lines of notes):
   - Read all 6 persona skill files (vault-librarian, -architect,
     -curator, -teacher, -historian, -janitor) -- prompts delightful?
     Cite well? Fail gracefully?
   - Read MCP server tool descriptions in mcp-server/dist/ (or src/) --
     full vault.{create,append,delete,rename,etc} surface, not just
     read/list/search. Are descriptions ergonomic for LLM tool selection
     or just technically accurate one-liners?
   - First conversation path: after install, what does user type first?
     Currently zero guidance. Need STARTERS.md or single line printed
     by setup (Try: /vault-librarian summarize my 3 most recent notes).
   - vault-mind.yaml: auto-generate from setup vs drop entirely in
     favor of env vars only.
2. Cold-start question (Curry raised at session end): new users
   without an existing vault. Three solution paths to evaluate:
   (a) Starter vault template in repo (seed 10-20 notes)
   (b) Add 7th persona vault-gardener -- I have nothing, help me
       start; uses existing vault.create / vault.append tools to
       write notes during conversation. Most aligned with Karpathy
       vault-as-conversation-precipitate philosophy.
   (c) Importer (Notion / Apple Notes / docx -> markdown). High
       effort, not differentiator.
   Audit should rank these.
3. Output: P0/P1/P2 fix list + verdict on vault-gardener persona.

### Known gaps still open (carried forward)

- [P0-product] docs/gif/demo.gif placeholder. README hero is literally
  a broken image link. Curry mentioned hyperframes
  (https://github.com/heygen-com/hyperframes) as the tool to generate
  this. Higher first-impression leverage than any remaining viewer
  or persona polish, per the pivot.
- [P1] vercel deploy + push v2-staging to origin + tag v2.0.0-rc1 --
  explicitly deferred this session per Curry. Once persona audit
  lands, push becomes the natural next checkpoint.
- [P2] In-browser file:// fallback for viewer (graceful fallback when
  fetch sample-graph.json blocked by CORS at file:// open). Inline
  sample-graph.json into index.html as window.SAMPLE_GRAPH=. Codex
  tier-1 pick but parked when viewer dropped from critical path.
- [P3] Bundle vs source drift CI check (no enforcement that
  mcp-server/bundle.js matches npm run bundle output)
- [P3] .gitattributes mark mcp-server/bundle.js linguist-generated
  so PR diffs collapse it
- [P3] terrarium yaml flat layout still unverified against actual
  KT parser

### v2-staging branch state

10 commits ahead of main. Working tree clean.
First action next session: cd D:/projects/obsidian-llm-wiki &&
git status && tail -100 progress.txt && open the 6 persona files
in skills/ to begin the audit.


## 2026-04-20 (late-late) -- persona + MCP tool description audit (read-only)

### What done (no code changes, read-only audit + strategy decisions)

Audited the 6 persona skills (vault-librarian, -architect, -curator,
-teacher, -historian, -janitor) against the actual MCP tool surface
(operations.ts + index.ts dispatch). Cross-referenced with
skills/mcp-tools-reference.md. Produced 12 P0/P1/P2 findings +
three strategic decisions. Nothing was written to code.

### Audit findings (12 items) -- to execute next session

P0 (correctness bugs that misfire today):
- #1 vault-curator.md describes "Stale Notes (90+ days)" section --
  vault.lint does NOT produce stale-notes data (handler returns only
  orphans / brokenLinks / emptyFiles / missingFrontmatter /
  duplicateTitles). Either add staleNotes to vault.lint, or strip
  the section from the persona.
- #2 skills/mcp-tools-reference.md has drifted from operations.ts:
    - doc says op "neq"/"not_exists", code expects "ne" and lacks
      "not_exists" entirely (valid ops at index.ts:358:
      eq, ne, gt, lt, gte, lte, contains, regex, exists)
    - vault.delete "force: true" documented, param does not exist
    - vault.search "context: N" documented, not implemented
    - vault.graph "type: backlinks/outgoing/both" documented,
      handler ignores the param entirely
  Best fix: delete parallel doc, auto-generate from operations.ts
  descriptions at build time (same SSOT pattern used by index.ts).
- #3 vault.graph declares enum param `type` (operations.ts:177) but
  handler ignores it (index.ts:391-421). Schema lies to the LLM.
  Either wire up or drop the param.
- #4 vault.externalSearch is registered but always throws
  "No external search engine configured" -- dead tool in tools/list
  degrades tool-selection quality. Gate registration on config or
  return {available:false} instead of throwing.

P1 (descriptions underinform tool selection):
- #5 Most tool descriptions are 3-word sentences.
  query.unified vs query.search vs query.explain all say "search";
  LLM cannot disambiguate multi-adapter-weighted vs filesystem-only
  vs top-10-with-3-line-context without reading the source.
  vault.enforceDiscipline + vault.reindex already show the right
  verbose style -- mimic for the others.
- #6 vault-architect.md template shows "Unresolved: K" but
  vault.graph returns only `orphans` (no unresolvedLinks count).
  Either template drops the line or graph op adds the field.
- #7 vault-historian.md asks the model to sort by mtime but mtime
  only comes from vault.stat (1 call per note; 20-result window =
  20 roundtrips). Add mtime to searchByFrontmatter results or add
  sort=mtime to vault.list.
- #8 No persona specifies MCP-unavailable fallback. Librarian has
  "no source found" -- others don't handle adapter-down. Add one-line
  fallback per persona per feedback-graduated rule 8 (memory-route-first).

P2 (ergonomics that compound):
- #9 (revised) Cold-start gap. Build vault-gardener persona. Headless,
  uses vault.create / vault.append, no plugin deps. See strategic
  decisions below -- the original proposed vault.import tool is
  KILLED; bulk ingest goes via Obsidian native CLI + Importer plugin.
- #10 No first-prompt UX after install. Emit one line from stderr
  startup banner (or print STARTERS.md): "Try: ask `what do I know
  about X` to invoke vault-librarian."
- #11 vault-janitor "Max 10 deletions per session" is persona-prompt-only,
  not server-enforced. Either enforce in vault.delete or accept as
  soft norm and document.
- #12 skills/mcp-tools-reference.md covered by #2 -- auto-gen or delete.

### Strategic decisions (Curry-confirmed this session)

1. KILL the proposed `vault.import` tool. We do not write format
   converters. Obsidian ships a NATIVE CLI
   (https://obsidian.md/cli, opt-in in Settings), binary `obsidian`,
   with commands daily/search/create/tasks/read/write/plugin:reload/
   devtools/eval/sync. `obsidian eval "app.commands.executeCommandById(
   'obsidian-importer:open-modal')"` drives any installed plugin.
   Bulk ingest of existing corpora goes through the native CLI +
   Obsidian Importer plugin + existing community plugins. Zero
   conversion code in this repo.

2. KEEP vault-gardener persona (original option b). Pure headless,
   uses vault.create / vault.append, no plugin dependency. Does NOT
   overlap with bulk import -- gardener is for conversational
   empty-vault seeding, not mass data landing. Option (a) starter
   template could ship as --seed flag on vault.init, low priority.

3. obsidian-vault-bridge (master branch, separate project) gets a
   strategic repositioning. NOT "WebSocket adapter for Obsidian
   plugins" (native CLI made that redundant). NEW role =
   multi-agent coordination + corpus-landing bus:
     a) multi-agent discovery (other agents find it)
     b) event stream subscription (vault + other source feeds
        push to subscribers)
     c) DB writes for language corpus cleaning workflows
        (memU pgvector / DuckDB / PG18 are existing backends)
   Corpus-landing pipeline is the primary consumer
   (hist-mat entbergen, Sprachwelt training data, Gemma 4 QLoRA,
   quant-terminal Trading-R1 distillation -- all bench on the
   same pattern). The Obsidian CLI's "app must be running"
   constraint is identical to bridge's, so CLI vs bridge is an
   implementation-mechanism choice, not a capability choice.
   Full design in separate memory file:
   ~/.claude/projects/C--Users-Administrator/memory/project_bridge_architecture_v2.md

### Headless / bridge boundary (confirmed)

- Headless-capable (Obsidian not required): pure filesystem ops --
  read/list/create/modify/append/delete/rename/search/searchByTag/
  searchByFrontmatter/graph/backlinks/lint/getMetadata/stat/exists.
  All the current vault.* tools stay headless.
- Bridge-required (Obsidian must be running): anything that hits
  Obsidian runtime -- plugin commands, Obsidian metadata cache,
  Templater rendering, Dataview queries, Importer conversions.
  Native CLI has the same constraint.

### What blocked

Nothing. All 12 fixes are file-scoped, minimal diff.

### Next step (next session opener)

1. Execute audit fixes on v2-staging in this order:
   P0 #1 (curator stale section) -> P0 #2 (mcp-tools-reference
   auto-gen or delete) -> P0 #3 (graph type param) -> P0 #4
   (externalSearch dead tool) -> P1 #5 (verbose descriptions for
   query.unified/search/explain + vault.search + vault.lint) ->
   P1 #6-#8 -> P2 #10-#11.
2. Build vault-gardener persona. Template after existing 6.
   Tool budget: vault.create, vault.append, vault.init (optional).
3. Re-evaluate skills/mcp-tools-reference.md -- likely delete
   and replace with `npm run generate-tools-doc` that emits from
   operations.ts.

### Deferred (separate session)

- obsidian-vault-bridge architecture v2 implementation (design
  captured in project_bridge_architecture_v2.md memory).
- docs/gif/demo.gif placeholder still.
- vercel deploy + push v2-staging to origin + tag v2.0.0-rc1
  (waits for audit fixes to land).

### v2-staging branch state

10 commits ahead of main. Working tree clean. This session made
zero code changes (read-only audit + strategy decisions only).
First action next session: cd D:/projects/obsidian-llm-wiki &&
git status && tail -200 progress.txt && start fixing P0 #1.
