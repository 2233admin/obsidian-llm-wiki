node_modules/
dist/
*.js.map
.env
vault-mind.yaml

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
_tmp/

# Local vault (user's own notes, never committed)
knowledge/
.cnxq/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Harness runtime state
.omc/
mcp-server/.omc/

# frontmatter_generator local state
compiler/fm_config.local.json
compiler/fm_work/
fm_work/

# Dogfood usage log (per-dev, not shared)
.dogfood.log

.dogfood.weekly.md

# vault-save session recap markers (per-dev)
.vault-save.log
.vault-save.failed.log

# LMVK L2 cost-guardrail state (per-machine daily $ spend counter, see
# compiler/cost_guard.py + scripts/lmvk-incremental-compile.ps1)
.vault-mind-cost-guard.json

# Bundle smoke-test artifacts (per-dev)
mcp-server/smoke.in
mcp-server/smoke.out
mcp-server/smoke.err

# link_discovery output (per-dev, regenerate with compiler/link_discovery.py)
.compile/

# Currency passes derived artifacts (Task 2): DERIVED, recomputable, NOT committed.
# Regenerate with: python compiler/kb_meta.py currency <vault> <topic> --apply
**/wiki/_current-truth.md
**/wiki/_supersession.md
**/wiki/_currency.json
**/wiki/_project-status.md
**/wiki/_triage.md
**/wiki/_initiative-status.md
**/wiki/_cycle-status.md
**/wiki/_work-os.canvas
**/_triage.canvas

# capture-hook (Task 4) per-machine idempotency state (default lives in
# ~/.vault-mind/; ignored here in case VAULT_CAPTURE_STATE points into the repo)
capture-hook.seen.log
**/capture-hook.seen.log

# Task 9 / 9A local project registry (MACHINE-LOCAL): workspace roots AND
# bindings are machine-specific paths, so the WHOLE dir is gitignored. A
# machine path must NEVER appear in any shared/committed markdown note (§0 #9).
# Holds workspace.json (workspace-roots) + local-bindings.json (entity -> path).
.vault-mind/

# Local planning / GSD artifacts (per-dev, not part of shipped product)
.planning/
.gitnexus
.repowise/
.repowise-workspace.yaml
.sentrux/
.cocoindex_code/
.claude/
.outreach/
/package-lock.json
agent_logs/
TASKS.md
workspaces/
uv.lock
